#use SDKDownloader;

# Download the Tizen SDK
#SDKDownloader::PrepareSDK("tizen-sdk", "2.4.0", "./artifacts");

# Build the plugin
#system("make build") == 0
#    or die "system \"make build\" failed: $?";
